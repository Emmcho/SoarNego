import { Outlet } from "react-router-dom";

const Layout = () => {
    return (
        <main className="App">
            {/* <Outlet /> */}
            Index page
        </main>
    )
}

export default Layout